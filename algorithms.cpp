#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <cstdio>
#include <ctime>

using namespace std;

double timer(){
	std::clock_t start;
    double duration;
    start = std::clock();

    // algo goes here 

    return (std::clock() - start ) / (double) CLOCKS_PER_SEC;
}

// Breadth-First Search

// Depth-First Search

// Binary Search

// Merge Sort

// Quick Sort  

int main(){

	return 0;
}