#include <iostream>
#include <vector>
#include <string>
#include <fstream>
#include <cstdio>
#include <ctime>

using namespace std;

double timer(){
	std::clock_t start;
    double duration;
    start = std::clock();

    // algo goes here 

    return (std::clock() - start ) / (double) CLOCKS_PER_SEC;
}

// O(1)

// O(log N)

// O(N)

// O(N log N)

// O(N^2)

// O(2^N)

// O(N!)


int main(){


	return 0;	
}